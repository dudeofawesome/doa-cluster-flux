apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cert-manager
commonLabels:
  app: cert-manager

resources:
  - namespace.yaml
  - release.yaml
  - issuer.yaml
  - secrets.yaml
  - certificates.yaml
