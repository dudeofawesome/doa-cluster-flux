## Project Overview

Kubernetes K3s cluster, managed via FluxCD, with Operator Lifecycle Manager v1, cert-manager, Traefik

Run non-basic commands from inside the devenv (`devenv shell --quiet -- {command}`), which will not work in a sandbox.
