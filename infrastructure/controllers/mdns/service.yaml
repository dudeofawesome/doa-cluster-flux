apiVersion: v1
kind: Service
metadata:
  name: avahi2dns
spec:
  # CoreDNS needs an address that does not depend on resolving a service name.
  clusterIP: 10.43.0.54
  selector:
    app: avahi2dns
  ports:
    - name: dns
      port: 53
      targetPort: dns
      protocol: UDP
