apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: airprint
spec:
  selector:
    matchLabels:
      component: airprint
  template:
    metadata:
      labels:
        component: airprint
    spec:
      automountServiceAccountToken: false
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      nodeSelector:
        feature.node.kubernetes.io/usb-ff_04e8_3434.present: 'true'
      initContainers:
        - name: seed-cups-state
          image: ghcr.io/dudeofawesome/cups:20260805@sha256:2dacb105e7b76157876f6498196b2568f9e0cd27970f34caecac547bdee3c94f
          command:
            - /bin/sh
            - -ec
            - |
              until test -S /run/dbus/system_bus_socket; do sleep 1; done
              cp -a /etc/cups/. /cups-config/
              sed "s/__NODE_NAME__/${NODE_NAME}/g" /config/cupsd.conf > /cups-config/cupsd.conf
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          volumeMounts:
            - name: cups-config
              mountPath: /cups-config
            - name: config
              mountPath: /config
              readOnly: true
            - name: system-bus
              mountPath: /run/dbus/system_bus_socket
              readOnly: true
      containers:
        - name: cups
          image: ghcr.io/dudeofawesome/cups:20260805@sha256:2dacb105e7b76157876f6498196b2568f9e0cd27970f34caecac547bdee3c94f
          command:
            - /bin/sh
            - -ec
            - |
              /usr/sbin/cupsd -f &
              cupsd_pid="$!"
              /config/bootstrap-cups.sh
              wait "$cupsd_pid"
          securityContext:
            privileged: true
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          ports:
            - name: ipp
              containerPort: 631
              protocol: TCP
          startupProbe:
            exec:
              command:
                - /bin/sh
                - -ec
                - |
                  test -S /run/dbus/system_bus_socket
                  lpstat -p "samsung-scx-4623-${NODE_NAME}" >/dev/null
                  openssl s_client -brief \
                    -connect "${NODE_NAME}.local:631" \
                    -servername "${NODE_NAME}.local" \
                    </dev/null >/dev/null 2>&1
            periodSeconds: 5
            failureThreshold: 24
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -ec
                - |
                  test -S /run/dbus/system_bus_socket
                  lpstat -p "samsung-scx-4623-${NODE_NAME}" >/dev/null
                  openssl s_client -brief \
                    -connect "${NODE_NAME}.local:631" \
                    -servername "${NODE_NAME}.local" \
                    </dev/null >/dev/null 2>&1
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command:
                - lpstat
                - -r
          volumeMounts:
            - name: usb
              mountPath: /dev/bus/usb
            - name: system-bus
              mountPath: /run/dbus/system_bus_socket
              readOnly: true
            - name: avahi
              mountPath: /run/avahi-daemon/socket
              readOnly: true
            - name: cups-config
              mountPath: /etc/cups
            - name: cups-spool
              mountPath: /var/spool/cups
            - name: config
              mountPath: /config
              readOnly: true
        - name: airsane
          image: docker.io/aguslr/airsane:20260805@sha256:0bff54500285e8c95230d72c1376a6f1a963a8cb5ba18d05945e0a535dd15cd9
          securityContext:
            privileged: true
          args:
            - --access-log=-
            - --debug=true
            - --listen-port=8090
            - --hotplug=true
            - --network-hotplug=false
            - --mdns-announce=true
            - --announce-secure=false
            - --announce-base-url=http://%H:8090
            - --web-interface=false
            - --reset-option=false
            - --disclose-version=false
            - --random-paths=false
            - --compatible-path=true
            - --local-scanners-only=true
          ports:
            - name: escl
              containerPort: 8090
              protocol: TCP
          startupProbe:
            httpGet:
              path: /eSCL/ScannerCapabilities
              port: escl
            periodSeconds: 5
            failureThreshold: 24
          readinessProbe:
            httpGet:
              path: /eSCL/ScannerCapabilities
              port: escl
          livenessProbe:
            httpGet:
              path: /eSCL/ScannerCapabilities
              port: escl
          volumeMounts:
            - name: usb
              mountPath: /dev/bus/usb
            - name: system-bus
              mountPath: /run/dbus/system_bus_socket
              readOnly: true
      volumes:
        - name: usb
          hostPath:
            path: /dev/bus/usb
            type: Directory
        - name: system-bus
          hostPath:
            path: /run/dbus/system_bus_socket
            type: Socket
        - name: avahi
          hostPath:
            path: /run/avahi-daemon/socket
            type: Socket
        - name: cups-config
          emptyDir: {}
        - name: cups-spool
          emptyDir: {}
        - name: config
          configMap:
            name: cups-config
            defaultMode: 0555
