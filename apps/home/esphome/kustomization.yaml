apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: esphome
commonLabels:
  app: esphome

resources:
  - namespace.yaml
  - config.yaml
  - storage.yaml
  - deployment.yaml
  - ingress.yaml
  - secrets.yaml
  - oidc-client.yaml
  - ../../../services/forward-auth

patches:
  - patch: |
      - op: replace
        path: /spec/forwardAuth/address
        value: http://forward-auth.esphome.svc.cluster.local:4181
    target:
      kind: Middleware
      name: forward-auth
