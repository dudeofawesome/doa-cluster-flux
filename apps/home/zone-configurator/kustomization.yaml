apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: zone-configurator
commonLabels:
  app: zone-configurator

resources:
  - namespace.yaml
  - storage.yaml
  - config.yaml
  - deployment.yaml
  - ingress.yaml
  - secrets.yaml
  - oidc-client.yaml
  - ../../../services/forward-auth

patches:
  - patch: |
      - op: replace
        path: /spec/forwardAuth/address
        value: http://forward-auth.zone-configurator.svc.cluster.local:4181
    target:
      kind: Middleware
      name: forward-auth
